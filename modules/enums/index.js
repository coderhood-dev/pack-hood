const Roles = require('./roles/roles');
const { Size, SIZE } = require('./sizes/sizes');
const { Status, STATUS } = require('./statuses/statuses');

module.exports = {
  Roles,
  Size,
  SIZE,
  Status,
  STATUS,
};
